#include <string>
#include <iostream> 
#include <map>

std::map <char, int> makePatternMap(const std::string&);

int findBoyerMoore(const std::string&, const std::string&);

int findBoyerMooreHorspool(std::string& , std::string& );
